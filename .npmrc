# ── npm configuration ─────────────────────────────────────────────────────
# Skip audit during install to prevent Hostinger's deployer from blocking
# on dev-only vulnerabilities (esbuild/vite dev server issues that don't
# affect the production build output).
audit=false

# Allow peer dependency mismatches without failing install
legacy-peer-deps=true
