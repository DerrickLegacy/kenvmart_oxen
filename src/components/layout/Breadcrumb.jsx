/**
 * Breadcrumb — intentionally disabled.
 * Returns null so breadcrumbs are hidden across the entire app
 * without needing to remove the import from each page.
 */
export default function PageBreadcrumb() {
  return null;
}
